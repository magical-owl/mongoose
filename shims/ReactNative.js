/**
 * Stub for react-native/Libraries/Renderer/shims/ReactNative
 *
 * This internal shim was removed in React Native 0.77 (New Architecture / Fabric).
 * react-native-reanimated 3.x still has a dead-code `else` branch that references it,
 * which Metro resolves statically at bundle time even though it never runs at runtime.
 * This empty stub satisfies the resolver without affecting behaviour.
 */
module.exports = {};
